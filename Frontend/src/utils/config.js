const config = {
    baseURL: 'http://127.0.0.1:5001', // Base API URL
    apiRoutes: {
        weeklyActivity: '/api/weekly-activity',
        students: '/api/students',
        compareWeeklyActivity: '/api/compare-weekly-activity',
    },
};

export default config;
